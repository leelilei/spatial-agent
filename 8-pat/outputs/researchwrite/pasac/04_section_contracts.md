# Section contracts

## Introduction

Must report the architecture-matched negative result before presenting PASAC. Must not imply that one keypoint per class already improves classification reliably.

## Related work

Must separate privileged pooling, semi-supervised keypoint localization, label-only part discovery, equivariant attention, and point-supervised segmentation. End with the narrow unresolved channel-calibration problem.

## Method

Must define K0 training, permitted sparse set, spatial cost, Hungarian assignment, matched keypoint loss, teacher preservation, and inference-time inputs. State that assignment is fold-local and trained without OOF images.

## Experiments

Must use architecture-matched and warm-start baselines, report BA plus attention hit rate, expose all gate thresholds, and keep official tests locked until cross-development validation.

## Limitations

Must acknowledge fixed named keypoints, CUB-centric first validation, added training cost, possible assignment instability, and the limited novelty audit.

