# Phase 1 Targeted Widened P1 Optimistic Recheck

Date: 2026-04-28

CSV companion:

- `assets/survey_paper/phase1/phase1_targeted_widened_p1_optimistic_recheck_2026-04-28.csv`

Purpose: adjudicate the three P1 reserve rows from the widened targeted screen and determine whether the widened-Core map can move beyond the `34`-row P0 state without flattening scope discipline.

## Summary

This optimistic recheck does not support admitting all three reserve rows.

| ID | Decision | Layer | Repr | Why |
|---|---|---|---|---|
| `TW-02` | exclude on scope boundary | `excluded` | `L4` | agent-facing network structure is plausible, but citation-network science-of-science sits outside the accepted widened spatial-social bridge scope |
| `TW-10` | keep adjacent | `adjacent` | not codable | VR is mainly modality/presentation; no clear agent-facing spatial context |
| `TW-12` | promote now | `bridge_core` | `L2` | socially situated NPC interaction in a virtual world is clear enough for widened bridge admission |

Net effect:

- widened-Core rows: `34 -> 35`
- widened-Core papers: `32 -> 33`
- `L2`: `7 -> 8`
- `L4`: unchanged at `1`
- `L5`: unchanged at `7`

This means the optimistic check clears the lower edge of the previously projected `35-37` range, but it does not justify pushing the stable map to `36-37` without an explicit scope expansion.

## TW-02: CiteAgent / Citation-Network Simulation

Decision: exclude from the stable widened-Core table on scope-boundary grounds.

Why it remains out:

- The paper is relevant to the broadened `L4` question because agents appear to operate over citation- and author-level network signals.
- However, the modeled environment is a science-of-science citation network rather than an online social world, embodied environment, or spatially situated interaction setting.
- That makes it weaker than `L4R-01`, `BK07`, and `BK08` for the survey's spatial-social bridge objective.

Claim use:

- It can be cited as scope-pressure evidence showing that `L4` can emerge in broader digital-network agent systems.
- It should not be counted in the main widened-Core map for this survey, because the project is not widening scope to include citation/research-community environments.

## TW-10: Conversing With AI Agents in VR

Decision: keep `Adjacent`.

Why it remains out:

- The study is clearly about VR-based human-agent interaction, but the available primary-source description emphasizes embodiment, conversational alignment, and modality rather than agent-facing spatial state.
- No strong evidence shows that room layout, object position, route structure, or other spatial context is operationalized in the agent prompt/state.

Claim use:

- Useful as background on VR conversational embodiment.
- Not usable as widened-Core evidence for spatial representation.

## TW-12: Virtual Tutoring System With LLM-Guided NPCs

Decision: promote now as `bridge_core / L2 / observed_effect`.

Why it qualifies:

- The system is a virtual-world tutoring environment with LLM-guided NPCs, gamified tasks, and user-NPC interaction inside a spatially explicit virtual scene.
- The primary-source description indicates a situated interaction workflow where users approach NPCs and trigger tutoring/dialogue behavior in-world.
- This is weaker than embodied `L5`, but it satisfies the widened bridge rule for socially situated human-agent interaction in a recognizable virtual environment.

Conservative coding:

- `core_layer = bridge_core`
- `agent_accessible_representation = L2`
- `environment_side_representation = 3D_engine`
- `evidence_status = observed_effect`
- `behavioral_scale = interaction`

Why not `L5`:

- The current evidence supports scene-level and proximity-based spatial context, not raw geometry, coordinates, or embodied navigation/state streams given to the agent.

## Recommendation

Update the stable widened-Core map to `35` rows by admitting `TW-12`.

Do not admit `TW-02` or `TW-10` to the stable widened-Core table.

Working interpretation after the optimistic pass:

- the widened bridge strategy succeeds in lifting the corpus into the `35`-row range;
- the gain comes mostly from VR/NPC/human-agent bridge evidence, especially `L2`;
- the `L4` scarcity result survives, because only `L4R-01` remains a clean admitted widened-bridge `L4` case.
