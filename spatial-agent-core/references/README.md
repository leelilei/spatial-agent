# References Moved

Research reference assets have been moved to the repository-level `assets/` directory.

- PDFs: `../../assets/papers/pdfs/`
- Reading notes: `../../assets/papers/reading_notes/`
- Generated paper list and BibTeX: `../../assets/papers/generated/`

This directory is kept only as a pointer so the code subproject does not also act as the asset archive.
