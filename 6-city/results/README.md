# Results

This folder stores raw and derived result artifacts for CityAgency experiments.

The canonical experiment ledger is:

```text
../RESULTS.md
```

For repeated CityIntent experiments, each run directory should contain
`run_config.json`, `runs.json`, `manifest.json`, `repeated_summary.md`, CSV
summary tables, and per-repeat trace/judge subdirectories.
