# Experiments

This folder is reserved for experiment orchestration scripts, command bundles,
and analysis notebooks that sit above the benchmark runner.

Use:

- `../results/` for raw and derived outputs
- `../docs/experiments/` for human-readable experiment notes
- `../RESULTS.md` for the canonical experiment ledger
