# Benchmarks

Machine-readable benchmark definitions, scenario packages, toy worlds, validators,
and scoring scripts.

- `cityintent_v0/`: first smoke-test track for CityAgency, focused on intention
  persistence and constraint-sensitive action.
