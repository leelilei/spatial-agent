# Experiments

This folder is reserved for experiment orchestration scripts, analysis notebooks, and
reproducible command bundles that sit above the core simulator in `../sim/`.

Use `../sim/runs/` for raw run outputs and `../RESULTS.md` for the experiment ledger.
